// TODO: implement SMTP (Nodemailer) logic in a later step

export async function sendApprovalEmail(to: string, name: string) {
  // placeholder for sending approval email
  console.log(`Would send approval email to ${to} (${name})`);
}

export async function sendDenialEmail(to: string, name: string) {
  // placeholder for sending denial email
  console.log(`Would send denial email to ${to} (${name})`);
}
